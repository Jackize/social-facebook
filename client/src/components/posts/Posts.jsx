import React from 'react';
import { Box } from '@mui/material';

const Posts = () => {
    return (
        <Box bgcolor="red" flex={2} p={2}>
            Posts
        </Box>
    );
};

export default Posts;
